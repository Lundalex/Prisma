using UnityEngine;

public class TaskPanelHelper : MonoBehaviour
{
    // TODO A
    // Implement a system for saving/retrieving task progress, such as correctly answered tasks.

    // TOOD B
    // if (no tasks (FullscreenWindowManager.showTasks == False)) -> Disable the object on which this script is located.
    // else -> :
    // 1. Create windows equal to the amount of tasks.
    // 2. Copy all texts, answer keys, grading, etc.
    // 3. Hook up task progress to the central save/retrieval system (TODO A)

    // TODO C
    // Hook up all tasks, scene setup, and grading SOs to a central TaskManager
}